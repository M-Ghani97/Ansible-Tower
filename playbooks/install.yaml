---
- name: "Install Packages"
  hosts: all
  gather_facts: false
  user: ubuntu

  tasks:
    - name: Adding install_packages Role
      include_role:
        name: ../roles/install_packages
      vars:
        install: "no"
